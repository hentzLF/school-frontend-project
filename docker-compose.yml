services:
  frontend:
    build: .
    container_name: agrimarket-frontend
    ports:
      - "3067:3000"
    environment:
      - NODE_ENV=production
      - BACKEND_URL=${BACKEND_URL}
    restart: unless-stopped